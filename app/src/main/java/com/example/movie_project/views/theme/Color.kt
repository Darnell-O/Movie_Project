package com.example.movie_project.views.theme

import androidx.compose.ui.graphics.Color

/**
 * Compose mirror of the brand colors defined in res/values/colors.xml,
 * kept in one place so screens don't hardcode hex values.
 */
val Iris = Color(0xFF5D3FD3)
val LightWhite = Color(0xFFFCFCFF)
val DarkGrey = Color(0xFF979797)