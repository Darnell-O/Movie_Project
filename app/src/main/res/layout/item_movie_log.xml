<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="entry"
            type="com.example.movie_project.data.local.MovieLogEntry" />

    </data>

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/card_movie_log_item"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/movie_log_item_margin"
        app:cardCornerRadius="@dimen/dp_8"
        app:cardElevation="@dimen/dp_2"
        app:cardBackgroundColor="@color/white">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="@dimen/movie_log_item_padding">

            <!-- Movie Title -->
            <TextView
                android:id="@+id/tv_movie_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:fontFamily="@font/montserrat_bold"
                android:textColor="@color/dark_grey"
                android:textSize="@dimen/movie_log_item_title_text_size"
                tools:text="The Shawshank Redemption" />

            <!-- Date Added -->
            <TextView
                android:id="@+id/tv_date_added"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/dp_5"
                android:fontFamily="@font/montserrat_regular"
                android:textColor="@color/dark_grey"
                android:textSize="@dimen/movie_log_item_date_text_size"
                tools:text="Added: April 6, 2026" />

        </LinearLayout>

    </com.google.android.material.card.MaterialCardView>

</layout>
